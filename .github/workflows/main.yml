name: FTP Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production 
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: server24.hostonnet.com
          port: 21
          username: edudemohon
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: "/public_html/"